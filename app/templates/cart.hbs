<h2>Cart</h2>

<div class='container'>
  <div class='row'>
    {{#each model as |product|}}
      {{#cart-list removeFromCart='removeFromCart' product=product}}
        {{outlet}}
      {{/cart-list}}
    {{/each}}
  </div>
</div>
